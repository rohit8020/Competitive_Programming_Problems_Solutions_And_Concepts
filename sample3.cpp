#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long int t;
    cin >> t;
    while (t--)
    {
        long long int n, x;
        cin >> n;
        while (cin >> x)
        {
            for (long long int i = 0; i < n; i++)
            {
                for (long long int j = 0; j < n; j++)
                {
                    if ()
                }
            }
        }

        return 0;
    }
